#include <stdio.h>
#include <stdint.h>

#include "soc.h"

long int n = 0;
const long int c = 1000;

int main() {
	while (1) {
		if (n % c==0) printf("SB Altera!\n");
		n++;
		SEG6->DATA = n;
		usleep(1000000/c);
	}

	return 0;
}
